/*
import React, { useState, useEffect } from "react";
import myData from '../scanner/completions.json';

myData.data.map((calculatedata, index) => {
    return (
        <div key={index}>
            <p>
                {calculatedata.data.map}
            </p>
        </div>
    )});
    
export default myData;
*/